local screenSize = { guiGetScreenSize() };

SDX = {
    screen = { w = screenSize[1], h = screenSize[2] },
    components = {
        list = { }
    },
    elements = {
        list = { }
    },
    utils = {
        models = { },
        extends = { }
    }
};
