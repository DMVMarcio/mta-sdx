SDX.assets.models.texture = { };

SDX.assets.models.texture.__index = SDX.assets.models.texture;

function SDX.assets.models.texture:onBeforeLoad(component_data)
    return true;
end

function SDX.assets.models.texture:onBeforeDestroy()
    return true;
end
